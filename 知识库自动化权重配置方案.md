# GPU 健康度平台 · AI + 知识库自动化权重配置方案

> 目标：在不动安全层（veto）、不改在线评分链路的前提下，用「知识库 + 标定器 + 受限 LLM」自动化地为不同场景生成、验证并上线评分权重，并随真实结果越用越准。

---

## 1. 目标与边界

**做什么**

- 为不同上下文（训练/推理、GPU 型号、卡龄阶段、集群）自动产出一套**评分权重 profile**。
- 每套 profile 是一个**带版本、可追溯、可回滚**的评分策略，由现有 `scoring_strategy` 承载。
- 用知识库 + 真实故障结果给权重提供**依据和解释**，而不是 LLM 黑盒拍数。

**绝不做（硬约束，违反即作废）**

- 不修改 `is_veto` / `veto_threshold`：一票否决永远是规则层，AI 不碰。
- 不在每分钟评分链路里动权重：在线 `scorer` 只读当前激活版本，逻辑零改动。
- 不做"每卡每轮变权重"：权重是缓慢演进、版本化的 profile，否则分数失去可比性、不可趋势、不可复现。

**一句话定位**：本方案是上一版「两速架构」中"离线权重顾问"的完整工程化，AI+知识库是其中"提案引擎"的内核。

---

## 2. 总体架构（两速分离）

```
离线（每天 / 按需 / 事件触发，慢）         在线（每分钟，快，零改动）
┌─────────────────────────────────┐       ┌──────────────────────┐
│ 1 统计聚合（CK 指标历史+结果标签）│       │ scorer.RunOnce       │
│ 2 标定器（确定性，回归/排序）     │       │  读 Redis 帧          │
│ 3 知识检索（RAG over 知识库）     │  产出  │  读 当前激活策略版本   │
│ 4 LLM 受限提案（带引用 JSON）     │ ────▶ │  打分 → 写快照         │
│ 5 护栏（夹紧/归一/限幅，veto不碰）│ 策略  │  veto 规则层不变       │
│ 6 落候选版本 + 灰度验证           │ 版本  └──────────────────────┘
│ 7 晋升/回滚 + 结果回灌知识库      │
└─────────────────────────────────┘
```

在线链路（`cmd/app` 里的 scorer cron）只感知"激活的策略版本号变了"，其余不变。

---

## 3. 核心原则（系统不变量）

1. **veto 不可变**：动态权重只重塑"非致命区间内"的分数排序与趋势。
2. **权重慢、版本化、可比**：一次调整 = 一个新策略版本（`scoring_strategy.version +1` 或新建候选策略），每条快照已记 `strategy_id`，天然可回溯。
3. **rigor 来自标签 + 回测**：权重是否更好，用它对**真实故障结果**的排序能力来判定，不是看 LLM 说得好不好。
4. **知识接地、可解释**：每条权重改动都引用具体知识库条目或真实事件，可审计、可质疑。
5. **一切提案都过护栏 + 灰度**：引用了知识库 ≠ 免审；初期还要人工审批。

---

## 4. 数据模型

### 4.1 现状与缺口

| 数据 | 现状 | 缺口 |
|---|---|---|
| 指标历史（时序） | 在 **CK** 里（你那张表）| 直接用，做回测的数据源 |
| 单卡评分 | `gpu_health_snapshot` **最新态覆盖写，无历史** | 回测不能依赖它；按需另建评分历史表 |
| 故障知识 | `fault_knowledge`（静态展示，与评分解耦）| 缺"真实事件 + 结果标签" |
| 策略/权重 | `scoring_strategy` + `strategy_metric_rule`，带 version | 缺"来源/决策"溯源字段 |

结论：**指标历史读 CK**；**结果标签和经验事件新建表**；知识库新建几张维表；策略复用现有结构、补溯源。

### 4.2 结果标签表（整套闭环的地基）

```go
// GPUOutcome 真实结果标签：让权重学习/回测有"金标准"
type GPUOutcome struct {
    ID          uint64    `gorm:"primaryKey;autoIncrement" json:"id"`
    GPUUUID     string    `gorm:"type:varchar(128);index;not null" json:"gpu_uuid"`
    ClusterID   uint64    `gorm:"index" json:"cluster_id"`
    EventType   string    `gorm:"type:varchar(32);index;not null" json:"event_type"` // fault/rma/false_alarm/recovered
    FailureMode string    `gorm:"type:varchar(64);index" json:"failure_mode"`        // 关联 kb_failure_mode.code
    OccurredAt  time.Time `gorm:"index;not null" json:"occurred_at"`
    DetectedBy  string    `gorm:"type:varchar(64)" json:"detected_by"`               // dcgm/人工/工单
    Notes       string    `gorm:"type:text" json:"notes"`
}
func (GPUOutcome) TableName() string { return "gpu_outcome" }
```

来源：DCGM 致命事件、运维工单、RMA 记录、人工标注。**这是优先级最高的一步**——没有它，标定器无米下锅，AI 只能面验证。

### 4.3 知识库（五类，结构化优先）

```go
// (1) 指标语义与阈值
type KBMetricSemantic struct {
    ID           uint64 `gorm:"primaryKey" json:"id"`
    MetricKey    string `gorm:"type:varchar(128);uniqueIndex" json:"metric_key"`
    Dimension    string `gorm:"type:varchar(32)" json:"dimension"`        // hardware/stability/performance/environment
    MetricType   string `gorm:"type:varchar(16)" json:"metric_type"`      // counter/gauge
    NormalMax    float64 `json:"normal_max"`
    WarnThresh   float64 `json:"warn_thresh"`
    CritThresh   float64 `json:"crit_thresh"`
    FailureModes string `gorm:"type:json" json:"failure_modes"`           // ["hbm_ecc","thermal"]
    VetoEligible bool   `json:"veto_eligible"`
    SourceRef    string `gorm:"type:varchar(255)" json:"source_ref"`      // DCGM 文档/论文出处
}
func (KBMetricSemantic) TableName() string { return "kb_metric_semantic" }

// (2) 故障模式 → 指标映射（FMEA 式）
type KBFailureMode struct {
    ID               uint64 `gorm:"primaryKey" json:"id"`
    Code             string `gorm:"type:varchar(64);uniqueIndex" json:"code"`     // hbm_ecc/nvlink_down/...
    Name             string `gorm:"type:varchar(128)" json:"name"`
    PrecursorMetrics string `gorm:"type:json" json:"precursor_metrics"`           // [{"metric":"...","direction":"up","lead_time_h":48}]
    XIDCodes         string `gorm:"type:json" json:"xid_codes"`                   // [48,64,79,95]
    Severity         string `gorm:"type:varchar(16)" json:"severity"`             // warning/critical/fatal
    Irreversible     bool   `json:"irreversible"`
    RMACriteria      string `gorm:"type:text" json:"rma_criteria"`
    EvidenceRef      string `gorm:"type:varchar(255)" json:"evidence_ref"`
}
func (KBFailureMode) TableName() string { return "kb_failure_mode" }

// (3) 历史事件/经验库（FaultKnowledge 是静态展示，这张是带结果的真实事件）
type FaultIncident struct {
    ID          uint64    `gorm:"primaryKey" json:"id"`
    GPUUUID     string    `gorm:"type:varchar(128);index" json:"gpu_uuid"`
    ClusterID   uint64    `gorm:"index" json:"cluster_id"`
    FailureMode string    `gorm:"type:varchar(64);index" json:"failure_mode"`
    PreMetrics  string    `gorm:"type:json" json:"pre_metrics"`     // 事发前被抬高的指标及数值快照
    LeadTimeH   float64   `json:"lead_time_h"`                      // 前兆提前量(小时)
    Resolution  string    `gorm:"type:text" json:"resolution"`
    OutcomeID   uint64    `gorm:"index" json:"outcome_id"`          // 关联 gpu_outcome
    OccurredAt  time.Time `gorm:"index" json:"occurred_at"`
}
func (FaultIncident) TableName() string { return "fault_incident" }

// (4) 运维 / profile 规则
type KBOpsRule struct {
    ID                 uint64 `gorm:"primaryKey" json:"id"`
    Scope              string `gorm:"type:varchar(64);index" json:"scope"`        // training/inference/h100/aged/...
    RuleText           string `gorm:"type:text" json:"rule_text"`
    AffectedDimensions string `gorm:"type:json" json:"affected_dimensions"`
    WeightHint         string `gorm:"type:json" json:"weight_hint"`               // {"hardware":"+","environment":"-"}
    Priority           int    `json:"priority"`
    Enabled            bool   `gorm:"default:true" json:"enabled"`
}
func (KBOpsRule) TableName() string { return "kb_ops_rule" }

// (5) 权重决策与灰度结果（闭环用）
type WeightDecision struct {
    ID                  uint64    `gorm:"primaryKey" json:"id"`
    Profile             string    `gorm:"type:varchar(64);index" json:"profile"`
    BaseStrategyID      uint64    `json:"base_strategy_id"`
    CandidateStrategyID uint64    `json:"candidate_strategy_id"`
    ProposedWeights     string    `gorm:"type:json" json:"proposed_weights"`
    CalibratorSuggest   string    `gorm:"type:json" json:"calibrator_suggest"`
    Rationale           string    `gorm:"type:text" json:"rationale"`
    Citations           string    `gorm:"type:json" json:"citations"`          // 引用的 KB 条目 id
    LLMConfidence       float64   `json:"llm_confidence"`
    Status              string    `gorm:"type:varchar(24);index" json:"status"` // proposed/approved/canary/promoted/rolled_back
    CanaryResult        string    `gorm:"type:json" json:"canary_result"`       // 灰度对比指标(回灌的客观结果)
    DecidedBy           string    `gorm:"type:varchar(64)" json:"decided_by"`
    CreatedAt           time.Time `json:"created_at"`
    DecidedAt           time.Time `json:"decided_at"`
}
func (WeightDecision) TableName() string { return "weight_decision" }
```

### 4.4 策略溯源（给现有 `ScoringStrategy` 补字段）

```go
Origin     string `gorm:"type:varchar(24);default:human" json:"origin"`   // human/advisor
DecisionID uint64 `gorm:"index" json:"decision_id"`                       // 关联 weight_decision
```

`strategy_metric_rule` 的 `is_veto` / `veto_threshold` 在整个流程中**只读**，禁止 advisor 写。

---

## 5. 标定器（确定性主干）

**输入**：CK 指标历史 + `gpu_outcome` 标签，构造样本「某卡某时刻的指标向量 → 是否在 N 天内变故障」。

**模型**：可解释模型——逻辑回归或 learning-to-rank（不要黑盒）。拟合后取各维度/各指标的系数，归一化为「权重基线建议」。

**输出**：一份 baseline 维度权重 + 置信度。

**冷启动**：标签不足（如某 profile 故障样本 < 阈值）时，本步**跳过**，权重基线退回当前激活版本，完全交给知识库+LLM 提案（但护栏更严、必须人工审批）。

```go
type CalibratorOutput struct {
    DimWeights map[string]float64 // {"hardware":0.50,...}
    SampleN    int                // 样本量(决定可信度)
    AUC        float64            // 区分故障/正常的能力
    Enough     bool               // 样本是否足够
}
```

---

## 6. 知识检索 + LLM 受限提案（RAG）

### 6.1 检索

给定 profile，按标签从知识库 SQL 检索（语料小、结构化，**不上向量库**，可审计）：

- `kb_metric_semantic`：全部 22 指标语义/阈值。
- `kb_failure_mode`：该集群近期出现过的故障模式（由 `fault_incident` 反查）。
- `kb_ops_rule`：`scope` 命中该 profile 的规则。
- `fault_incident`：该 profile 近期相似事件 Top-K。

自由文本（事件描述）若需相似检索，再可选加 embedding；否则按 `failure_mode` / `cluster_id` 结构化召回即可。

### 6.2 LLM I/O 契约（严格 JSON，低温度）

**输入**（组装进 prompt）：当前激活权重、标定器建议、检索到的 KB 条目（带 id）、各指标近窗分布与近阈值频率、各维度触发 veto 构成。

**系统提示要点**：你是评分权重顾问；只能调整四个维度权重；不得改动 veto；每条改动必须引用给定 KB 条目 id；输出严格 JSON。

**输出契约**：

```json
{
  "profile": "training-h100",
  "dimension_weights": {"hardware":0.50,"stability":0.28,"performance":0.14,"environment":0.08},
  "changes": [
    {"target":"hardware","from":0.45,"to":0.50,"reason":"DBE 前兆相关性最高","cite":["kb_failure_mode:3","fault_incident:118"]}
  ],
  "rationale": "近30天 DBE 与后续故障强相关；温度整体偏高但与故障弱相关……",
  "confidence": 0.7
}
```

复用现有 `internal/service/assistant/llm_client.go`；解析后做 JSON Schema 校验，任何缺字段/越界/无引用 → 拒绝该提案。

---

## 7. 护栏（确定性，落库前强制）

```go
func ApplyGuardrails(base, proposed map[string]float64) (map[string]float64, error) {
    out := map[string]float64{}
    for dim, w := range proposed {
        lo, hi := base[dim]*0.6, base[dim]*1.4   // 单维相对基线 ±40%
        out[dim] = clamp(w, lo, hi)
    }
    out = renormalize(out)                        // 四维强制和为 1
    if maxAbsDelta(base, out) > 0.10 {            // 单次最多挪 10 个百分点
        return base, errors.New("变动过大，需人工确认或拆多次")
    }
    // veto 规则不在入参里 → 天然不被触碰
    return out, nil
}
```

外加：同一 profile 调整**冷却期**（如 7 天内不重复调）；置信度低于阈值强制人工审批。

---

## 8. 版本化与灰度

1. **落候选**：护栏通过 → 以候选身份新建一条 `scoring_strategy`（`origin=advisor`, `decision_id=...`），维度权重写入 `dimension_weights`，指标规则从 base 复制（**含原样 veto**），仅改维度权重。
2. **灰度/影子**：候选策略对该 profile 的一小撮卡（或整体只算不替换）评分一段时间，与激活版本并行。
3. **晋升判据**（客观、基于标签）：候选对"后续真故障卡"的排序能力不劣于现版本——用 `precision@k` 或 AUC（候选分排序 vs `gpu_outcome`）衡量，且分数不抖动。达标→晋升；否则→回滚。
4. **晋升 = 绑定**：用现有 `卡 > 集群 > 默认` 绑定，把该 profile 的卡指向新策略；scorer 下一轮自动热加载。
5. **回滚**：解绑/切回旧版本即可（版本化天然可逆）。

灰度结果写回 `weight_decision.canary_result`。

---

## 9. 闭环（越用越准）

把两类客观结果回灌知识库：

- **真实结果标签** → `gpu_outcome` + `fault_incident`（含事发前指标快照），喂给下一轮标定器和检索。
- **灰度对比结果** → `weight_decision.canary_result`，作为"这次调整到底好不好"的外部信号。

**防自证循环**：决策库必须记**灰度真实结果**而非只记提案；检索给 LLM 的"过往决策"要带上其灰度成败，避免 AI 强化自己的错误判断。

---

## 10. 上下文 profile（"根据情况"的安全落地）

不让单卡权重漂移，而是维护少量 profile，按现有绑定切换：

| profile 维度 | 取值示例 | 绑定方式 |
|---|---|---|
| 工作负载 | training / inference | 集群级策略绑定 |
| GPU 型号 | H100 / A100 | 卡级或集群级 |
| 卡龄阶段 | 婴儿期 / 稳定期 / 磨损期 | 卡级 |

每个 profile 一版权重，组合用 `卡 > 集群 > 默认` 优先级解析——几乎零新增机制。

---

## 11. 接口与调度

```
POST /api/v1/advisor/propose?profile=training-h100   # 跑一轮：聚合+标定+检索+提案+护栏，产出候选(不上线)
GET  /api/v1/advisor/decisions                        # 决策列表(含 rationale/citations/canary)
POST /api/v1/advisor/decisions/:id/approve            # 人工审批 → 进灰度/晋升
POST /api/v1/advisor/decisions/:id/rollback           # 回滚
```

调度：`@daily` 触发 propose（产候选待审）；灰度评估单独 cron；也支持"事件触发"（某 profile 新出现一类故障模式时）。

---

## 12. 在代码中的落点

```
internal/service/weightadvisor/
  aggregator.go   统计聚合（读 CK 指标历史 + gpu_outcome）
  calibrator.go   确定性标定（回归/排序）
  retriever.go    知识库检索（SQL by 标签）
  proposer.go     组装 prompt + 调 assistant.LLMClient + 解析校验 JSON
  guardrails.go   ApplyGuardrails
  canary.go       影子评分 + 晋升判据
  service.go      编排整条 loop
internal/model/    新增 GPUOutcome / KBMetricSemantic / KBFailureMode /
                   FaultIncident / KBOpsRule / WeightDecision；ScoringStrategy 补 Origin/DecisionID
internal/repository/  新增 advisor_repo.go / kb_repo.go；复用 StrategyRepo/HealthRepo/MetricRepo/FaultRepo
internal/handler/     新增 advisor_handler.go
cmd/app/              注册 advisor cron（与 scorer/ckloader 同进程）
```

**不动的**：`scorer_service.go`、`scoring/aggregator.go`、`scoring/curve.go`（评分与曲线/veto 逻辑零改动）。

---

## 13. 安全与治理

- **veto 永远只读**：advisor 任何路径都不写 `is_veto`/`veto_threshold`。
- **知识库要治理**：录入/复核/版本化是系统的一部分；错的或过时的 KB 会系统性带偏权重（GIGO）。
- **审批门默认开**：第一阶段「AI 提案 + 人确认 + 版本化」，成熟后再放开灰度自动晋升。
- **全程可审计**：每个激活权重都能追溯到 decision → 引用的 KB 条目 / 真实事件 / 标定结果。
- **LLM 不确定性**：低温度 + 严格 JSON Schema 校验 + 护栏 + 灰度/人工门，多重兜底。

---

## 14. 分阶段落地

| 阶段 | 内容 | 产出 |
|---|---|---|
| P0 | 建 `gpu_outcome`，开始收集结果标签；录入静态知识（指标语义、故障模式）| 标签 + 知识库地基 |
| P1 | aggregator + calibrator | 确定性权重建议（人工应用）|
| P2 | retriever + proposer + guardrails | 带引用的 LLM 提案，人工审批上线 |
| P3 | canary 影子评分 + 晋升判据 | 灰度验证、达标自动晋升 |
| P4 | 闭环回灌（标签 + 灰度结果）| 自我改进，越用越准 |

**关键提醒**：P0 的结果标签是整套系统的前提，应最先做、持续做；它既喂标定器，又是 `fault_incident` 经验库的内容，是闭环的地基。

---

## 15. 风险与边界（诚实）

- 没有结果标签时，本系统只能做到"知识接地的有据猜测"（面验证），不是"经过验证的标定"。标签是 rigor 的来源。
- 动态权重只优化**分诊/趋势分数**对你机群故障模式的贴合度；它**仍不是故障概率**，更不是 RUL。
- 知识库质量决定上限；它需要人持续维护，不是一次性工程。
- 安全永远靠规则层（veto）兜底；本系统对 go/no-go 决策不构成、也不应构成影响。
