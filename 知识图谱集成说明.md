# 故障知识图谱 — 集成说明

本模块新增 4 个 Go 文件 + 1 个前端页面，另需对 6 处已有文件做小改动。
**所有改动都不触碰评分链路**：不修改 `scoring` 包、不修改 `accel_metric_scoring` /
`strategy_metric_rule` / `gpu_health_snapshot` 的读写逻辑。

---

## 一、新增文件（直接放入对应目录）

| 文件 | 放置路径 |
|---|---|
| `knowledge_graph.go` | `internal/model/knowledge_graph.go` |
| `kg_repo.go` | `internal/repository/kg_repo.go` |
| `kg_service.go` | `internal/service/kg_service.go` |
| `kg_handler.go` | `internal/handler/kg_handler.go` |
| `FaultKnowledge.vue` | `web/src/views/FaultKnowledge.vue`（覆盖原文件） |

---

## 二、需要修改的已有文件

### 2.1 `internal/repository/db.go` — 注册两张新表

在 `AutoMigrate` 的模型列表末尾追加：

```go
func AutoMigrate(db *gorm.DB) error {
	return db.AutoMigrate(
		&model.MetricDefinition{},
		&model.ScoringStrategy{},
		&model.StrategyMetricRule{},
		&model.Cluster{},
		&model.Node{},
		&model.GPUCard{},
		&model.GPUHealthSnapshot{},
		&model.ClusterHealthSummary{},
		&model.FaultKnowledge{},
		&model.FaultRule{},
		&model.FaultEvent{},
		&model.AIConversation{},
		&model.AIMessage{},
		&model.KGNode{}, // 新增
		&model.KGEdge{}, // 新增
	)
}
```

同时建议开启 GORM 的错误翻译，让唯一键冲突返回 `gorm.ErrDuplicatedKey`
而不是靠字符串匹配（`kg_repo.IsDuplicate` 两条路都兼容，开不开都能跑，
但开了更可靠）：

```go
	gormCfg := &gorm.Config{TranslateError: true}
```

### 2.2 `internal/seed/seed.go` — **不要**把新表加进清表列表

`Reset` 里的 `tables` 切片**保持原样**，不要加 `kg_node` / `kg_edge`。

图谱内容是人工沉淀的知识资产，一旦被 `seed.reset=true` 清掉就找不回来了。
这两张表由 AutoMigrate 建，只增不删。

### 2.3 `internal/router/router.go` — 装配与注册路由

在仓储实例化那一段追加：

```go
	kgRepo := repository.NewKGRepo(db)
```

在 handler 实例化那一段追加：

```go
	kgSvc := service.NewKGService(kgRepo, metricRepo, faultRepo)
	kgH := handler.NewKGHandler(kgSvc)
```

在 `api := r.Group("/api/v1")` 的块内追加：

```go
		// ── 故障知识图谱（与评分链路无关，纯知识管理）──
		api.GET("/kg/meta", kgH.Meta)
		api.GET("/kg/graph", kgH.Graph)
		api.GET("/kg/metric-options", kgH.MetricOptions)

		api.GET("/kg/nodes", kgH.ListNodes)
		api.POST("/kg/nodes", kgH.CreateNode)
		api.GET("/kg/nodes/:id", kgH.NodeDetail)
		api.PUT("/kg/nodes/:id", kgH.UpdateNode)
		api.DELETE("/kg/nodes/:id", kgH.DeleteNode)
		api.GET("/kg/nodes/:id/neighbors", kgH.Neighbors)

		api.POST("/kg/edges", kgH.CreateEdge)
		api.PUT("/kg/edges/:id", kgH.UpdateEdge)
		api.DELETE("/kg/edges/:id", kgH.DeleteEdge)

		api.POST("/kg/import/fault-knowledge", kgH.ImportFaultKnowledge)
```

> 注意 gin 的路由冲突规则：同一层级的路径参数必须同名。上面全部用 `:id`，
> 与已有的 `/faults/knowledge/:id` 不在同一前缀下，不会冲突。

### 2.4 `web/src/api/index.ts` — 新增接口

在 `api` 对象里追加：

```ts
  // ── 故障知识图谱 ──
  kgMeta: () => http.get<any, any>("/kg/meta"),
  kgGraph: (params?: any) => http.get<any, any>("/kg/graph", { params }),
  kgNeighbors: (id: number, params?: any) =>
    http.get<any, any>(`/kg/nodes/${id}/neighbors`, { params }),
  kgNodes: (params?: any) => http.get<any, any>("/kg/nodes", { params }),
  kgNodeDetail: (id: number) => http.get<any, any>(`/kg/nodes/${id}`),
  kgCreateNode: (data: any) => http.post<any, any>("/kg/nodes", data),
  kgUpdateNode: (id: number, data: any) => http.put<any, any>(`/kg/nodes/${id}`, data),
  kgDeleteNode: (id: number) => http.delete<any, any>(`/kg/nodes/${id}`),
  kgCreateEdge: (data: any) => http.post<any, any>("/kg/edges", data),
  kgUpdateEdge: (id: number, data: any) => http.put<any, any>(`/kg/edges/${id}`, data),
  kgDeleteEdge: (id: number) => http.delete<any, any>(`/kg/edges/${id}`),
  kgMetricOptions: (params?: any) => http.get<any, any>("/kg/metric-options", { params }),
  kgImportFaultKnowledge: () => http.post<any, any>("/kg/import/fault-knowledge"),
```

**前置检查**：页面的错误处理依赖 `e.response.status`（区分 409/404）。
如果 `web/src/api/index.ts` 里的 axios 响应拦截器把非 2xx 的错误吞掉了或者
重新包装过，需要确认 reject 出来的对象仍带 `response` 字段。原始 axios
的默认行为是带的；如果项目里加了拦截器做了转换，把 `FaultKnowledge.vue` 里
`handleApiError` 的取值路径改成对应的形状即可。

### 2.5 `web/vite.config.ts` — 预构建声明（可选但建议）

`optimizeDeps.include` 里已经有 `echarts/charts`，新用到的 `GraphChart`
在同一个子路径下，一般不用改。若 dev 环境出现「临时发现新依赖 → 重新优化 → 504」，
删掉 `web/node_modules/.vite` 重启即可。

### 2.6 菜单入口

路由 `/fault/knowledge` 已存在，指向的就是被覆盖的 `FaultKnowledge.vue`，
无需改 `router/index.ts`。若侧边栏菜单名还叫「故障知识」，可改成「故障知识图谱」。

---

## 三、数据库

不需要写 SQL，两张表由 AutoMigrate 建。若生产环境已关掉 `auto_migrate`，
用下面的 DDL 手工建：

```sql
CREATE TABLE IF NOT EXISTS `kg_node` (
  `id`          BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `node_key`    VARCHAR(191)    NOT NULL COMMENT '业务唯一键，形如 fault:XID_48',
  `node_type`   VARCHAR(32)     NOT NULL COMMENT 'fault/concept/scope/metric',
  `name`        VARCHAR(191)    NOT NULL,
  `summary`     VARCHAR(512)    NULL,
  `description` TEXT            NULL,
  `severity`    VARCHAR(16)     NULL COMMENT '仅 fault 类型有效',
  `props`       JSON            NULL,
  `version`     INT             NOT NULL DEFAULT 1 COMMENT '乐观锁版本号',
  `created_at`  DATETIME(3)     NULL,
  `updated_at`  DATETIME(3)     NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_kg_node_key` (`node_key`),
  KEY `idx_kg_node_type` (`node_type`),
  KEY `idx_kg_node_name` (`name`),
  KEY `idx_kg_node_severity` (`severity`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
  COMMENT='故障知识图谱-节点';

CREATE TABLE IF NOT EXISTS `kg_edge` (
  `id`         BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `from_id`    BIGINT UNSIGNED NOT NULL,
  `to_id`      BIGINT UNSIGNED NOT NULL,
  `rel_type`   VARCHAR(32)     NOT NULL,
  `label`      VARCHAR(128)    NULL,
  `weight`     DOUBLE          NOT NULL DEFAULT 1,
  `props`      JSON            NULL,
  `version`    INT             NOT NULL DEFAULT 1,
  `created_at` DATETIME(3)     NULL,
  `updated_at` DATETIME(3)     NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_kg_edge` (`from_id`,`to_id`,`rel_type`),
  KEY `idx_kg_edge_from` (`from_id`),
  KEY `idx_kg_edge_to` (`to_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
  COMMENT='故障知识图谱-关系';
```

### 可选：影响范围节点起步数据

「影响范围」没有可自动推导的来源，需要人工建。下面这段可以直接执行，
用 `INSERT IGNORE` 保证重复执行不会报错：

```sql
INSERT IGNORE INTO `kg_node`
  (`node_key`,`node_type`,`name`,`summary`,`props`,`version`,`created_at`,`updated_at`)
VALUES
  ('scope:单卡不可用','scope','单卡不可用','该卡无法被任务调度使用','{}',1,NOW(3),NOW(3)),
  ('scope:单卡性能下降','scope','单卡性能下降','卡仍可用但算力或带宽降级','{}',1,NOW(3),NOW(3)),
  ('scope:整机不可用','scope','整机不可用','节点上全部加速卡受影响','{}',1,NOW(3),NOW(3)),
  ('scope:通信域降级','scope','通信域降级','NVLink/HCCS 域内互连带宽或可靠性下降','{}',1,NOW(3),NOW(3)),
  ('scope:训练任务中断','scope','训练任务中断','正在运行的训练任务会失败或挂起','{}',1,NOW(3),NOW(3)),
  ('scope:跨机通信中断','scope','跨机通信中断','RoCE/IB 链路异常导致集合通信失败','{}',1,NOW(3),NOW(3));
```

---

## 四、验证步骤

### 4.1 编译与启动

```bash
go build ./...
go vet ./...
```

启动后确认日志里出现 `kg_node` / `kg_edge` 的建表记录（AutoMigrate 开启时）。

### 4.2 接口自测

```bash
BASE=http://127.0.0.1:8080/api/v1

# 元数据：应返回 4 种节点类型、6 种关系类型
curl -s $BASE/kg/meta | jq .

# 从已有故障知识条目导入
curl -s -XPOST $BASE/kg/import/fault-knowledge | jq .

# 子图
curl -s "$BASE/kg/graph?limit=50" | jq '.data.nodes|length, .data.edges|length'
```

### 4.3 必须验证的边界情况

| 场景 | 期望结果 |
|---|---|
| 建一条 `INDICATED_BY` 从「影响范围」连到「指标」 | 400，提示关系不允许该端点组合 |
| 同一对节点重复建同类型关系 | 409，提示已存在 |
| 节点连向自己 | 400 |
| 用旧的 `version` 更新节点 | 409，提示已被他人修改 |
| 删除一个有 5 条关系的节点 | 200，返回 `deleted_edges: 5`，且 `kg_edge` 中无残留 |
| `depth=99` 展开 | 自动截断到 3 跳，不报错 |
| `limit=99999` 查询 | 自动截断到 1000 |
| `props` 传 `"[1,2]"` | 400，提示必须是 JSON 对象 |

悬空边检查（应该永远返回 0 行）：

```sql
SELECT COUNT(*) FROM kg_edge e
LEFT JOIN kg_node a ON a.id = e.from_id
LEFT JOIN kg_node b ON b.id = e.to_id
WHERE a.id IS NULL OR b.id IS NULL;
```

### 4.4 确认没有影响评分

导入并编辑图谱后，重新跑一轮评分，下面三项应与改动前完全一致：

```sql
SELECT card_type, COUNT(*) FROM accel_metric_scoring WHERE is_health_key = 1 GROUP BY card_type;
SELECT s.code, COUNT(r.id) FROM scoring_strategy s
  LEFT JOIN strategy_metric_rule r ON r.strategy_id = s.id GROUP BY s.id;
SELECT level, COUNT(*), ROUND(AVG(score),2) FROM gpu_health_snapshot GROUP BY level;
```

---

## 五、设计说明（便于后续维护）

**为什么用属性图而不是给 `fault_knowledge` 加几个字段。**
故障、概念、影响范围、指标之间是多对多且会不断演化的关系。用固定列表达，
每加一种关联就要加一张关联表；用节点+边表达，加一种关联只是在
`model.RelTypes` 里追加一行，前端图例、校验、下拉都自动生效。

**为什么不引入 Neo4j。**
当前规模是数百节点、数千边的领域知识，查询模式只有「筛选子图」和
「N 跳邻域」两种。MySQL 加上 `(from_id,to_id,rel_type)` 唯一索引和两个单列索引
完全够用，BFS 在服务层做且跳数、节点数都有硬上限。为此额外部署一套图数据库
在预发和生产各占一份资源，性价比不成立。若将来节点数上万且需要路径查询、
社区发现这类算法，再迁移不迟——届时 `KGService` 就是天然的适配层。

**schema 约束是这套模型的核心。**
`model.RelTypes[].Pairs` 定义了每种关系允许的端点类型组合。没有这层约束，
属性图很快会退化成一张谁都能连谁的乱图，几个月后就没人敢信里面的数据。
`RELATED_TO` 是唯一不限端点的兜底关系，用于表达暂时归不了类的关联。

**弱引用而非外键。**
指标节点通过 `props.metric_name` 指向 `accel_metric_scoring.metric_name`，
不建外键。这是为了保证「指标定义怎么改都不会影响图谱，图谱怎么改也不会
影响评分」。代价是可能出现悬空引用，所以 `NodeDetail` 接口会实时校验并在
前端标记为「指标定义中未找到」，让维护者自己决定是改名还是删节点。

**并发安全。**
节点和边都有 `version` 乐观锁，两个人同时编辑同一条数据时，后提交的会收到
409 而不是静默覆盖。删除节点时用 `SELECT ... FOR UPDATE` 锁住节点行再删边，
建边时同样锁住两个端点——这两把锁配合起来，杜绝了「一边在删节点、一边在建
指向它的边」产生悬空边的竞态。

**规模保护。**
`MaxGraphNodes=1000`、`DefaultGraphCap=300`、`MaxExpandDepth=3` 三个常量
限制了单次返回的规模。超限时返回 `truncated: true`，前端会提示用户。
力导向布局在 300 个节点以内是流畅的，再多就既卡又看不清了。

---

## 六、后续可以做但这版没做的

- **节点合并/改名传播**：目前改名不会影响 `node_key`，需要手工改；
  如果将来导入来源变多，建议加一个「合并重复节点」的运维接口。
- **变更审计**：现在没有 `created_by` / `updated_by`，因为项目还没有登录体系。
  接入鉴权后，在两张表加这两列并从 context 取用户即可。
- **图谱与故障事件联动**：`fault_event` 里的故障可以自动定位到图谱节点，
  在故障详情页展示「这类故障通常影响哪些范围、看哪些指标」。这需要在
  `fault_event` 和 `kg_node` 之间建一条弱引用，同样不碰评分。
- **导出/导入 JSON**：便于在不同环境之间搬运图谱内容。
