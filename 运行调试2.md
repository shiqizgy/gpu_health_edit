# GPU 健康度平台 — 运行调试手册

> **架构简述**：单进程多角色（api + loader + scorer），从 ClickHouse 采集 GPU 指标 → 内存直传评分 → 结果写 MySQL → API 提供给前端展示。
> 已移除 Redis 依赖，数据流全程不经中间件。

---

## 一、项目结构
- cmd/app/main.go        唯一入口，通过配置 roles 决定启用哪些角色
- configs/pre/           预发环境配置（Rancher 部署用）
- configs/local/         本地开发配置
- internal/              业务代码
- web/                   前端（Vue 3 + Naive UI）
- deployments/           容器化部署文件


---

## 二、本地开发环境

### 前置条件

- Go 1.22+
- Node.js 18+ / pnpm
- Podman（或 Docker）用于启动 MySQL

### 2.1 启动 MySQL

```bash
cd deployments
podman compose -f podman-compose.yml up -d mysql
# 等待 30 秒后验证
podman exec gpuhealth-mysql mysqladmin ping -uroot -prootpass
```

本地开发不需要 Redis 和 ClickHouse 容器（CK 直连远端）。

### 2.2 灌入种子数据（首次或重置时）
```
#先启动 app 让 AutoMigrate 建表
go run ./cmd/app -config configs/local/config.yaml
#看到 "api 已启用" 后 Ctrl+C 停掉

# 灌种子
cat scripts/seed.sql | podman exec -i gpuhealth-mysql mysql -uroot -prootpass --default-character-set=utf8mb4 gpu_health

# 验证
podman exec gpuhealth-mysql mysql -uroot -prootpass gpu_health -e \
"SELECT COUNT(*) FROM metric_definition; SELECT COUNT(*) FROM strategy_metric_rule;"
```

### 2.3 启动后端
```
go run ./cmd/app -config configs/local/config.yaml
```
configs/local/config.yaml 中 app.roles: []（空）表示同时启动 api + loader + scorer。

### 2.4 启动前端
```
cd web
pnpm install
pnpm dev
# 访问 http://localhost:5173
```

---

## 三、预发/生产部署（Rancher）
### 3.1 配置文件 configs/pre/config.yaml
```
app:
  roles: []                # 空=全角色（api + loader + scorer）

server:
  addr: ":8080"
  mode: release

mysql:
  dsn: "<生产 MySQL DSN>"
  max_open: 50
  max_idle: 10
  auto_migrate: true       # 首次部署设 true 自动建表，稳定后可改 false

scorer:
  cron: "@every 1m"
  strategy_code: "default"
  workers: 0               # 0=自动取 CPU 核数

assistant:
  enabled: false

ck:
  addr: "ckpub332.olap.jd.com:2000"   # host:port 合在一起
  database: monitor
  username: read_db_monitor
  password: OvOYQmI39wzWTUsxPs-E
  table: "<填写 GPU 指标表名>"          # 必填！
  cron: "@every 1m"
  window_sec: 300
```
关键配置说明：

- ck.addr 必须是 host:port 格式，不可分开写
- ck.table 不能为空，否则 SQL 语法错误
- roles: [] 空数组表示启用全部角色

### 3.2 容器构建
```
podman build -t gpuhealth-backend -f deployments/Containerfile.backend .
```
镜像入口为：/app/app -config /app/configs/pre/config.yaml

### 3.3 部署验证
```
# 健康检查
curl http://<pod-ip>:8080/api/v1/dashboard/overview

# 查看评分是否在运行（日志中应有）：
# "CK 采集完成：xxx 张卡"
# "评分完成：xxx 张卡"
```
---

## 四、数据流架构
```
ClickHouse（生产指标）
│
▼  CKLoaderService.Collect()（每分钟）
内存 []MetricFrame
│
▼  ScorerService.RunOnceWith()
评分引擎（策略 + 曲线 + 一票否决）
│
▼  写入 MySQL
gpu_health_snapshot（单卡快照，upsert）
cluster_health_summary（集群聚合，预计算）
fault_event（故障池，有状态）
│
▼
Gin API (:8080) ──► 前端 Vue (:5173)
```
---

## 五、角色说明
| 角色 | 功能 | 关键服务 |
|------|------|----------|
| loader | 从 CK 拉取指标 + 自动同步拓扑 | CKLoaderService |
| scorer | 按策略评分 + 故障检测 | ScorerService |
| api | HTTP API 供前端调用 | Gin Router |

配置 app.roles:
- [] 或不配：启用全部角色（推荐单实例部署）
- ["api"]：仅 API
- ["loader", "scorer"]：仅后台任务

---
## 六、常用排查命令
```
# API 直测
curl http://127.0.0.1:8080/api/v1/dashboard/overview
curl http://127.0.0.1:8080/api/v1/health/clusters

# 数据库数据量
mysql -h<host> -u<user> -p<pwd> <db> -e "
SELECT COUNT(*) AS gpu_cards FROM gpu_card;
SELECT COUNT(*) AS snapshots FROM gpu_health_snapshot;
SELECT COUNT(*) AS clusters FROM cluster_health_summary;
"

# 查看某集群的 GPU 评分
curl http://127.0.0.1:8080/api/v1/health/clusters/1/gpus
```
---
## 七、重置数据库
```
# 方式1：删库重建（数据全丢）
mysql -h<host> -u<user> -p<pwd> -e "DROP DATABASE <db>; CREATE DATABASE <db> CHARACTER SET utf8mb4;"
# 然后重启 app（auto_migrate:true 会自动建表），再灌种子

# 方式2：只清评分数据（保留拓扑和策略）
mysql -h<host> -u<user> -p<pwd> <db> -e "
TRUNCATE TABLE gpu_health_snapshot;
TRUNCATE TABLE cluster_health_summary;
TRUNCATE TABLE fault_event;
"
```
---
## 八、注意事项
- CK 表名必填：ck.table 为空会导致启动后采集报 SQL 语法错误
- 首次部署：auto_migrate: true 会自动建表 + 灌种子数据后即可开始评分
- 评分延迟：启动后需等 1 分钟（第一个 cron tick）才会有评分数据
- CORS：生产环境如果前端不在同一域名下，需修改 router.go 中的 AllowOrigins
- Vendor 策略：如果 CK 中有华为 NPU 数据，需在配置中添加 vendor_strategy 映射
