# GPU 健康度平台 — 运行调试手册

> **前置**:Podman 已装好、镜像已能拉取、Go / Node / pnpm 已装。
> **项目根目录** = `gpu-health-platform\gpu-health-platform`(注意是里层那个,解压多套了一层)。
> 环境:Windows + PowerShell + Podman。

---

## 一、一次性环境准备(只做一次)

```powershell
# PowerShell 允许运行脚本(否则 pnpm 起不来)
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned

# pnpm 换国内源 + 放宽超时(否则 echarts 等大包下载超时)
pnpm config set registry https://registry.npmmirror.com
pnpm config set fetch-timeout 600000
pnpm config set fetch-retries 5
```

### 镜像加速(如果 podman 拉 docker.io 慢)

进虚拟机改**真正生效的那个文件**,不是主配置:

```powershell
podman machine ssh
sudo nano /etc/containers/registries.conf.d/999-podman-machine.conf
```

文件末尾追加以下内容,保存(`Ctrl+O` 回车 / `Ctrl+X` 退出),然后 `exit` 回主机:

```toml
[[registry]]
location = "docker.io"
[[registry.mirror]]
location = "docker.m.daocloud.io"
[[registry.mirror]]
location = "docker.1panel.live"
```

---

## 二、启动依赖容器

```powershell
重新插入种子数据
make down    # 停止并清理容器
make up      # 重新启动容器
make seed    # 重新插入种子数据
```

```powershell
cd "C:\Users\shiqi\Desktop\gpu-health-platform\gpu-health-platform"
podman machine start

# 1. 停止并删除容器和数据卷
podman-compose -f deployments/podman-compose.yml down -v

# 2. 重新启动
podman-compose -f deployments/podman-compose.yml up -d

podman ps     # 确认 gpuhealth-mysql、gpuhealth-redis 都是 Up
```

等 20-30 秒,测连通:

```powershell
podman exec gpuhealth-mysql mysqladmin ping -uroot -prootpass   # 期望: mysqld is alive
podman exec gpuhealth-redis redis-cli ping                       # 期望: PONG
```

---

## 三、建表 + 灌种子(首次或重置数据库时)

### 第 1 步:启 server 自动建表(新窗口,常驻)

```powershell
cd "C:\Users\shiqi\Desktop\gpu-health-platform\gpu-health-platform"
go run ./cmd/server
# 看到 "API 服务启动于 :8080" 即建表完成,窗口别关

#CK建表
podman exec -it gpuhealth-ck clickhouse-client -q "                                                                                                                                                                                                            
CREATE TABLE IF NOT EXISTS gpu_metrics (
    timestamp DateTime,
    ip String,
    sn String,
    source String,
    mib String,
    tags String,
    value Float64,
    dt Date,
    gpu_node_group String
) ENGINE = MergeTree()
PARTITION BY dt
ORDER BY (timestamp, sn, mib)
"
```

### 第 2 步:灌种子(另开窗口)

> **关键点**:用 `-Encoding UTF8 -Raw` 防中文乱码;清空要关外键检查。

```powershell
cd "C:\Users\shiqi\Desktop\gpu-health-platform\gpu-health-platform"

# 清空(关外键检查,否则报 foreign key 错)
podman exec gpuhealth-mysql mysql -uroot -prootpass gpu_health -e "SET FOREIGN_KEY_CHECKS=0; TRUNCATE TABLE metric_definition; TRUNCATE TABLE scoring_strategy; TRUNCATE TABLE strategy_metric_rule; TRUNCATE TABLE fault_knowledge; SET FOREIGN_KEY_CHECKS=1;"

# 灌入(必须带 -Encoding UTF8 -Raw)
Get-Content scripts\seed.sql -Encoding UTF8 -Raw | podman exec -i gpuhealth-mysql mysql -uroot -prootpass gpu_health
#mac中为
cat scripts/seed.sql | podman exec -i gpuhealth-mysql mysql -uroot -prootpass --default-character-set=utf8mb4 gpu_health

# 验证:应返回 22 / 22 / 4
podman exec gpuhealth-mysql mysql -uroot -prootpass gpu_health -e "SELECT COUNT(*) FROM metric_definition; SELECT COUNT(*) FROM strategy_metric_rule; SELECT COUNT(*) FROM fault_knowledge;"
```

---

## 四、启动后台服务(各开一个常驻窗口)

```powershell
# 窗口:仿真(建 2000 卡拓扑 + 每分钟造数据写 Redis)
cd "C:\Users\shiqi\Desktop\gpu-health-platform\gpu-health-platform"
go run ./cmd/simulator
# 日志 "仿真生成完成：2000 张卡"

# 窗口:评分(每分钟从 Redis 拉指标算分写 MySQL)
cd "C:\Users\shiqi\Desktop\gpu-health-platform\gpu-health-platform"
go run ./cmd/scorer
# 日志 "评分完成：2000 张卡"
```

> 数据要等 1-2 分钟(仿真、评分各跑完一轮)才齐全。

---

## 五、启动前端

```powershell
cd "C:\Users\shiqi\Desktop\gpu-health-platform\gpu-health-platform\web"
pnpm install      # 首次装依赖,大包慢可多跑几次(断点续传)
pnpm dev          # 看到 Local: http://localhost:5173
```

浏览器打开 `http://localhost:5173`。

---

## 六、最终窗口清单(共 4 个常驻 + 1 个空闲)

| 窗口 | 命令 |
|---|---|
| server | `go run ./cmd/server` |
| simulator | `go run ./cmd/simulator` |
| scorer | `go run ./cmd/scorer` |
| 前端 | `cd web; pnpm dev` |
| 空闲 | 灌种子 / 查数据库用 |

---

## 七、日常重启(数据库已建好,不用再灌种子)

```powershell
# 1. 起容器
podman machine start
cd "C:\Users\shiqi\Desktop\gpu-health-platform\gpu-health-platform"
podman compose -f deployments/podman-compose.yml up -d

# 2. 四个窗口分别跑
go run ./cmd/server
go run ./cmd/simulator
go run ./cmd/scorer
cd web; pnpm dev
```

---

## 八、验证 / 排查命令

```powershell
# 后端 API 直测(绕过前端,最关键的排查手段)
Invoke-RestMethod http://127.0.0.1:8080/api/v1/dashboard/overview

# 数据库数据量
podman exec gpuhealth-mysql mysql -uroot -prootpass gpu_health -e "SELECT COUNT(*) FROM gpu_card; SELECT COUNT(*) FROM gpu_health_snapshot;"

# Redis 是否有指标
podman exec gpuhealth-redis redis-cli KEYS "gpu:metrics:*" | Select-Object -First 5

# 看表结构
podman exec gpuhealth-mysql mysql -uroot -prootpass gpu_health -e "DESCRIBE fault_knowledge;"
```

**前端排查**:F12 → Console 看红色报错;Network 看有没有 `/api/...` 请求及其状态码。

---

## 九、踩过的坑速查

| 现象 | 原因 | 解决 |
|---|---|---|
| `make 无法识别` | Windows 没 make | 直接用对应的原始命令(见上) |
| 找不到 compose 文件 | 解压多套了一层目录 | cd 到里层 `gpu-health-platform\gpu-health-platform` |
| 拉镜像慢 / 429 | docker.io 限流 | 进虚拟机改 `999-podman-machine.conf` 加国内源 |
| 加速器配了不生效 | 改错文件 | 改 `registries.conf.d/999-podman-machine.conf`,不是主 `registries.conf` |
| 灌种子中文变 `???` | PowerShell 编码 | `Get-Content ... -Encoding UTF8 -Raw` |
| `Invalid JSON / document is empty` | JSON 列插了空串 | seed.sql 里空 `''` 改 `NULL` |
| `Cannot truncate ... foreign key` | 外键约束 | TRUNCATE 前后包 `SET FOREIGN_KEY_CHECKS=0/1` |
| `Unknown column 'xid_code'` | 列名 GORM 转成 `x_id_code` | seed.sql 列名改 `x_id_code` |
| `pnpm.ps1 禁止运行脚本` | PS 执行策略 | `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned` |
| pnpm 下载超时 | npm 官方源慢 | 换 `registry.npmmirror.com` + 调大 timeout |
| 页面空 + 路由切换失效 | axios 少剥一层 data | 拦截器返回 `r.data.data`(已修复) |

---

## 十、关停

```powershell
# 四个服务窗口各按 Ctrl+C

# 停容器(保留数据)
podman compose -f deployments/podman-compose.yml down

# 停容器并删数据(彻底重置,下次要重新灌种子)
podman compose -f deployments/podman-compose.yml down -v
```

---

## 十一、重置数据库(数据乱了想重来)

```powershell
podman exec gpuhealth-mysql mysql -uroot -prootpass -e "DROP DATABASE gpu_health; CREATE DATABASE gpu_health CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
# 然后重启 server 建表 → 重新灌种子(见第三节)
```

---

## 附:架构与数据流速览

```
仿真服务(2000卡/分钟)  ──写──►  Redis(每卡最新全量指标)
                                      │
                                      ▼ 读
                              评分服务(每分钟)
                                      │ 按策略算分
                                      ▼
                              MySQL(单卡快照 + 集群汇总预聚合)
                                      │
                                      ▼
                              API 服务(Gin) ──► 前端(Vue, :5173)
```

- **server** (`:8080`):Gin API,首次启动自动建表(GORM AutoMigrate)
- **simulator**:每分钟造 2000 卡指标写 Redis(计数器型指标只累加)
- **scorer**:每分钟从 Redis 拉指标,按评分策略算分,写 MySQL 快照 + 集群汇总
- **前端** (`:5173`):Vue 3 + Naive UI,7 个页面
